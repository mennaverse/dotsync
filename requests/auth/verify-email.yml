info:
  name: verify-email
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/auth/verify-email?token="
  params:
    - name: token
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
