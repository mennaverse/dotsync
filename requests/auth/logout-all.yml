info:
  name: logout-all
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/auth/logout-all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
