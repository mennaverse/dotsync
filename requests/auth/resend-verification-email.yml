info:
  name: resend-verification-email
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/auth/resend-verification-email"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
