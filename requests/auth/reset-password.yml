info:
  name: reset-password
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
