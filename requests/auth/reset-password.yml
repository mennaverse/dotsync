info:
  name: reset-password
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password?token="
  params:
    - name: token
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "new_password": "123qwe"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
