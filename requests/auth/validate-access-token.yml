info:
  name: validate-access-token
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/auth/validate-access-token"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
