export function Field({ label }: { label: string; children: React.ReactNode }) {
  return (
    <div className="mb-4">
      <label className="text-gray-700 text-sm font-bold mb-2">{label}</label>
      {/* TODO: field */}
    </div>
  );
}
